#include "main.h"
/**
  *_abs - this function computes the absolute value of an integer
  *@t: integer
  *Return: absolute value
  */
int _abs(int t)
{
	if (t >= 0)
		return (t);
	else
		return (-t);
}
